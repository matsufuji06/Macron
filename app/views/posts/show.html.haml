= @post.food
= image_tag @post.image.url if @post.image?
= @post.calorie
= @post.carbo
= @post.fat
= @post.protein
= @post.weight
= @post.date
- if user_signed_in? && current_user.id == @post.user_id
  =link_to "削除する", post_path(@post.id), method: :delete
-# .formGroup
-#   .formGroup__comment
-#     .formGroup__comment--list
-#       - if current_user
-#         = form_for (model: [@post, @comment], local: true) do |f|
-#           = f.text_area :text, placeholder:  "コメントする", rows: "2"
-#           = f.submit "SEND"
-#       - else
-#         %strong.p "*** コメントの投稿には新規登録/ログインが必要です ***"
-#     .formGroup__comment--create
-#       - if @comments
-#         = @comments.each do |comment|
-#           %p= comment.text

